{
  "name": "causality-learning", "private": true, "version": "0.2.0",
  "description": "Static, interactive course in causal inference and semiparametric geometry",
  "scripts": {"test": "node --test tests/*.test.cjs", "serve": "python3 -m http.server 8765 --bind 127.0.0.1", "test:browser": "node tests/browser.cjs"},
  "engines": {"node": ">=22"}
}
